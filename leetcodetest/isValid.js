const isValid = (s) =>{
    const map = new map([
        ['(',')'],
        ['{','}'],
        ['[',']'],
        ['<','>']
    ])
    let arr = s.split('')
    if(arr.length / 2  != 0) return false
    for(let i = 0;i < arr.length; i++){
        
    }
    console.log(arr)
}
isValid("{})>")
